# .github/workflows/scrape-nepse-fundamentals.yml

name: Scrape NEPSE Fundamentals & Upload CSV

on:
  schedule:
    - cron: '0 12 * * *'   # Runs daily at 12:00 UTC (~5:45 PM Nepal time)
  workflow_dispatch:       # Allows manual trigger from GitHub UI

jobs:
  scrape-and-upload:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-colab-selenium[undetected] nepse-scraper requests pandas

      - name: Run NEPSE Fundamentals Scraper
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}   # GitHub token with repo scope
        run: |
          python scrape_fundamentals.py

      - name: Commit and push if changed
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add *.csv
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update fundamental data $(date +'%Y-%m-%d')" && git push)
